/** Fixed, blurred luxury light orbs sitting behind everything. */
export function AmbientOrbs() {
  return (
    <div aria-hidden className="ak-orbs">
      <span className="ak-orb ak-orb-gold" />
      <span className="ak-orb ak-orb-rose" />
    </div>
  );
}
